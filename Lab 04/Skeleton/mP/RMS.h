/* function to calculate RMS value. Returns RMS value of array "data"
 * float* data - pointer to array containing the raw data
 * int n - number of samples in the array
 */


#include <math.h>

float RMS(float *data, int n);
